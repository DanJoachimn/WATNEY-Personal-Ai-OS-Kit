---
type: brain-drawer-readme
generated_by: claude-code
created: YYYY-MM-DD
updated: YYYY-MM-DD
---

# _Brain/_pending/ — the holding area

The notability gate says: only create a full `people/` or `companies/` page for an entity mentioned **2+ times**, or **once with substantive content**. Everything else — a name dropped once in passing, a company mentioned without detail — lands here as a candidate.

**How it works:**
- A singleton mention → the AI drops a one-line candidate note here (`Pending — [name] — [where it was mentioned] [Source: ...]`).
- On the **second mention**, the AI promotes the candidate to a full page in the right drawer and removes it from `_pending/`.
- [PARTNER_NAME] can also promote manually anytime, or clear out candidates that turned out to be noise.

This keeps the filing cabinet's real drawers (people/companies/concepts) high-signal — only entities that actually matter get a page, and nothing notable gets lost in the meantime.
