<?xml version="1.0" encoding="UTF-8"?>
<!--
  Telegram poller launchd job for [AI_NAME].

  Save this to:
    ~/Library/LaunchAgents/com.[partner].[ai-name].telegram-poller.plist

  Before loading, find-replace:
    [PARTNER_USERNAME]   -> macOS username (run `whoami` to get it)
    [AI_NAME]            -> the AI's folder name (e.g. mira)
    [PARTNER]            -> short identifier (e.g. julie)

  Then load with:
    launchctl load   ~/Library/LaunchAgents/com.[partner].[ai-name].telegram-poller.plist
    launchctl kickstart -k gui/$(id -u)/com.[partner].[ai-name].telegram-poller

  Unload (to stop):
    launchctl unload ~/Library/LaunchAgents/com.[partner].[ai-name].telegram-poller.plist

  Polls every 60 seconds. Logs to ~/[ai-name]/logs/telegram-poller.{out,err}.
-->
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.[PARTNER].[AI_NAME].telegram-poller</string>

  <key>ProgramArguments</key>
  <array>
    <string>/Users/[PARTNER_USERNAME]/[AI_NAME]/scripts/poll-telegram.sh</string>
  </array>

  <key>EnvironmentVariables</key>
  <dict>
    <key>AI_NAME</key>
    <string>[AI_NAME]</string>
    <key>HOME</key>
    <string>/Users/[PARTNER_USERNAME]</string>
    <key>PATH</key>
    <string>/opt/homebrew/bin:/usr/local/bin:/usr/bin:/bin</string>
  </dict>

  <key>StartInterval</key>
  <integer>60</integer>

  <key>RunAtLoad</key>
  <true/>

  <key>KeepAlive</key>
  <false/>

  <key>StandardOutPath</key>
  <string>/Users/[PARTNER_USERNAME]/[AI_NAME]/logs/telegram-poller.out</string>

  <key>StandardErrorPath</key>
  <string>/Users/[PARTNER_USERNAME]/[AI_NAME]/logs/telegram-poller.err</string>
</dict>
</plist>
